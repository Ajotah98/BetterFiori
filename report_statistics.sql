select * from shell;
select 
    'card' as component,
    2      as columns;
select
    'Hours per center' as title,
    '/statistics/hours_per_center.sql?_sqlpage_embed' as embed;
select
    'Hours per laborcode' as title,
    '/statistics/hours_per_laborcode.sql?_sqlpage_embed' as embed;
select
    'Hours per day' as title,
    '/statistics/hours_per_day.sql?_sqlpage_embed' as embed;